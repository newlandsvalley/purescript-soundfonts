{
  "private": true,
  "scripts": {
    "build": "pulp build",
    "test": "pulp test",
    "example": "pulp build --main Example.Main --include example/src --to example/dist/purescript-soundfonts.js"
  },
  "devDependencies": {
    "pulp": "^12.3.0",
    "purescript": "^0.12.5",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
