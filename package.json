{
  "private": true,
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "example": "spago bundle --package example --outfile dist/purescript-soundfonts.js --bundle-type app --platform browser",
    "example-old": "spago -x ./example.dhall bundle-app --main Example.Main --to example/dist/purescript-soundfonts.js" 
  },
  "devDependencies": {
    "purescript": "^0.15.15",
    "purescript-psa": "^0.6.0",
    "spago": "^1.0.3"
  }
}
