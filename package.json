{
  "private": true,
  "scripts": {
    "build": "spago build",
    "test": "spago test",
    "example": "spago bundle --package example --outfile dist/purescript-soundfonts.js --bundle-type app --platform browser"
  },
  "devDependencies": {
    "purescript": "^0.15.16",
    "purescript-psa": "^0.6.0",
    "spago": "^1.0.3"
  }
}
